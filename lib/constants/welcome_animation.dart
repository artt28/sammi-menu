class DurationConstants {
  DurationConstants._();
  static const Duration fadeDuration = Duration(seconds: 4);
  static const Duration imageChangeInterval = Duration(seconds: 2);
  static const Duration transitionDuration = Duration(milliseconds: 600);

// booking endpoints
}
